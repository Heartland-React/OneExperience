import React from "react";
import ReactDOM from "react-dom";
import Button from "../src/Button";

ReactDOM.render(
	<Button onClick={() => {}} text='Test' type='primary' />,
	document.getElementById("root")
);
